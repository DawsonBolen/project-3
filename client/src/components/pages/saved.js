import React from 'react';

const Saved = () => {
    return (
        <div>
            <h2>Saved Posts</h2>
            {/* Display saved posts here */}
        </div>
    );
}

export default Saved;